import { Typography } from 'antd';

const { Title } = Typography;

export default function Dashboard() {
  return (
    <div>
      <Title level={3}>Executive Dashboard</Title>
      <p>Dashboard KPI widgets will be built in Phase 4.</p>
    </div>
  );
}
